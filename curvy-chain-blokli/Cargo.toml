[package]
name = "curvy-chain-blokli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Blokli GraphQL adapters for the Curvy shielded pool SDK"

# Blokli GraphQL adapters.

[dependencies]
curvy-chain-api = { workspace = true }
curvy-types = { workspace = true }
async-trait = { workspace = true }
reqwest = { workspace = true }
serde_json = { workspace = true }
# Hex32 note ids / batch indices are converted to the decimal Dec the seam speaks.
num-bigint = { workspace = true }
